# Add project specific ProGuard rules here.
# minifyEnabled is false for v1.0 (see build.gradle), so this file is inert
# until release minification is turned on — kept here so that switch is a
# one-line change later, not a rules-writing exercise under deadline.

-keep class com.flashtalk.aac.data.** { *; }
